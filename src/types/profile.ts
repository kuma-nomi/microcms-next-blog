import type { Image } from '@/types/common'

export type Profile = {
  image: Image
  detail: string
  skills: string
  createdAt: string
  updatedAt: string
  publishedAt: string
  revisedAt: string
}
